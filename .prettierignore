# Prose is a human's call. Prettier's markdown formatter rewrites bullet characters and reflows
# tables, which produces large diffs on documents nobody asked it to restyle — including the
# backlog and the design docs. Code formatting is settled by .prettierrc; markdown is left alone.
*.md

# Generated by `nestjs-trpc generate` and overwritten on every router change. Formatting it only
# creates a diff that the next generation throws away.
packages/api-contract/src/server.ts
